#!/bin/bash
[ "${debug}" = "true" ] && set -x

function hlp(){
( cat << EOF
    lst lists hosts+ips using lxc-ls --fancy without headers 
EOF
)
}

function doc(){
( cat << EOF
$(hlp)
EOF
)
}

function lst(){
    lxc-ls --fancy -Fname,ipv4 | tail -n +2 > /dev/stdout
}


$CMD
